import React from 'react';
import UpdateModal from '../../components/auth/UpdateModal';

const UpdateForm = () => {
  const onSubmitHand = (data) => {};

  return <UpdateModal onSubmitHand={onSubmitHand}></UpdateModal>;
};

export default UpdateForm;
